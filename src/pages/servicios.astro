---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import SectionTitle from "../components/SectionTitle.astro";
import ServiceCard from "../components/ServiceCard.astro";
import Toast from "../components/Toast.astro";
import WhatsAppButton from "../components/WhatsAppButton.astro";

import { getCollection } from "astro:content";

const services = await getCollection("services");
---

<Layout
  title="Servicios - Taller Industrial Brenes"
  description="Servicios especializados en soldadura, portones metálicos, estructuras y reparaciones. Más de 20 años de experiencia en Costa Rica."
>
  <Navbar />

  <main>
    <!-- Hero Section -->
    <section
      class="bg-gradient-to-r from-[#1e293b] to-[#1a1a1a] text-white py-20"
    >
      <div class="container mx-auto px-4 text-center">
        <h1 class="text-5xl font-bold mb-6 scroll-animate fade-up">
          Nuestros Servicios
        </h1>
        <p
          class="text-xl opacity-90 max-w-3xl mx-auto scroll-animate fade-up"
          style="transition-delay:.15s"
        >
          Soluciones integrales en metalurgia con más de 20 años de experiencia
        </p>
      </div>
    </section>

    <!-- Divisor hacia Grid de Servicios -->
    <div
      class="h-16 relative bg-gradient-to-r from-[#1e293b] to-[#1a1a1a] -mt-px -mb-px z-10 w-full"
    >
      <div
        class="absolute inset-0 bg-gradient-to-b from-transparent to-[#f9fafb]"
      >
      </div>
    </div>

    <!-- Grid de Servicios -->
    <section class="py-20 bg-gray-50">
      <div class="container mx-auto px-4">
        <div
          id="services-container"
          class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 stagger-children"
        >
          {
            services.map((service, index) => (
              <div data-category={service.data.category}>
                <ServiceCard
                  title={service.data.title}
                  description={service.data.description}
                  icon={service.data.icon || "🔧"}
                  delay={index * 0.1}
                />
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Divisor hacia CTA -->
    <div
      class="h-16 relative bg-gradient-to-r from-[#1a1a1a] to-[#1e293b] -mt-px -mb-px z-10 w-full"
    >
      <div
        class="absolute inset-0 bg-gradient-to-b from-[#f9fafb] to-transparent"
      >
      </div>
    </div>

    <!-- CTA -->
    <section
      class="py-20 bg-gradient-to-r from-[#1a1a1a] to-[#1e293b] text-white"
    >
      <div class="container mx-auto px-4 text-center">
        <h2 class="text-4xl font-bold mb-6 scroll-animate fade-up">
          ¿Necesita alguno de nuestros servicios?
        </h2>
        <p
          class="text-xl mb-8 opacity-90 scroll-animate fade-up"
          style="transition-delay:.15s"
        >
          Contáctanos para una evaluación gratuita
        </p>
        <a
          href="/contacto"
          class="scroll-animate scale-in inline-flex items-center gap-3 bg-white text-dark-800 hover:bg-gray-100 font-bold text-lg px-10 py-4 rounded-full shadow-xl hover:shadow-white/25 transform hover:scale-105 transition-all duration-300 border border-gray-200"
          style="transition-delay:.3s"
        >
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"
            ></path>
          </svg>
          Solicitar Cotización
          <svg
            class="w-4 h-4 ml-1 transition-transform group-hover:translate-x-1"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        </a>
      </div>
    </section>
  </main>

  <Footer />
  <Toast />
  <WhatsAppButton />
</Layout>
