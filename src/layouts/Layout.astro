---
export interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const { title, description = "Taller Industrial Brenes - Soluciones en portones, estructuras metálicas y soldadura especializada en Costa Rica", ogImage = "/og-image.png" } = Astro.props;

const siteUrl = "https://www.tallerindustrialbrenes.com";
const canonicalURL = new URL(Astro.url.pathname, Astro.site || siteUrl);
const fullOgImage = new URL(ogImage, siteUrl).toString();
---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <!-- Favicon -->
  <link rel="icon" href="/favicon.ico" sizes="any">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">

    
    <meta name="generator" content={Astro.generator} />
    
    <!-- SEO Tags -->
    <title>{title}</title>
    <link rel="canonical" href={canonicalURL} />
    
    <!-- Open Graph / Facebook / WhatsApp -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={fullOgImage} />
    <meta property="og:image:secure_url" content={fullOgImage} />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={`${title} - Taller Industrial Brenes`} />
    <meta property="og:site_name" content="Taller Industrial Brenes" />
    <meta property="og:locale" content="es_CR" />
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={fullOgImage} />
    <meta property="twitter:image:alt" content={`${title} - Taller Industrial Brenes`} />
    
    <!-- Organization Schema -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Organization",
        "name": "Taller Industrial Brenes",
        "alternateName": "Taller Industrial Brenes y Asociados de Grecia S.A",
        "url": "https://www.tallerindustrialbrenes.com",
        "logo": "https://www.tallerindustrialbrenes.com/LogoTallerBrenes.png",
        "description": "Taller industrial especializado en portones automáticos, estructuras metálicas y soldadura especializada en Costa Rica",
        "contactPoint": {
          "@type": "ContactPoint",
          "telephone": "+506 8931-8268",
          "contactType": "customer service",
          "areaServed": "CR",
          "availableLanguage": "es"
        },
        "sameAs": [
          "https://www.facebook.com/tallerindustrialbrenes",
          "https://www.instagram.com/tallerindustrialbrenes/"
        ]
      }
    </script>

    <!-- Local Business Schema -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "LocalBusiness",
        "name": "Taller Industrial Brenes y Asociados de Grecia S.A",
        "image": "https://www.tallerindustrialbrenes.com/LogoTallerBrenes.png",
        "logo": "https://www.tallerindustrialbrenes.com/LogoTallerBrenes.png",
        "@id": "https://www.tallerindustrialbrenes.com",
        "url": "https://www.tallerindustrialbrenes.com",
        "telephone": "+506 8931-8268",
        "address": {
          "@type": "PostalAddress",
          "streetAddress": "Rincón de Arias",
          "addressLocality": "Grecia",
          "addressCountry": "CR"
        },
        "openingHoursSpecification": [
          {
            "@type": "OpeningHoursSpecification",
            "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
            "opens": "06:00",
            "closes": "17:00"
          },
          {
            "@type": "OpeningHoursSpecification", 
            "dayOfWeek": "Saturday",
            "opens": "06:00",
            "closes": "12:00"
          }
        ]
      }
    </script>
  </head>
  <body>
    <slot />
    <script>
      import('../lib/intersection');
      import('../lib/filters');
      
      document.addEventListener('DOMContentLoaded', () => {
        import('../lib/intersection').then(({ initScrollAnimations, initLazyLoading }) => {
          initScrollAnimations();
          initLazyLoading();
        });
      });
    </script>
  </body>
</html>